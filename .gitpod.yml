# This configuration file was automatically generated by Gitpod.
# Please adjust to your needs (see https://www.gitpod.io/docs/introduction/learn-gitpod/gitpod-yaml)
# and commit this file to your remote git repository to share the goodness with others.

# Learn more from ready-to-use templates: https://www.gitpod.io/docs/introduction/getting-started/quickstart

tasks:
  - name: Terminal Test
    before: echo "running -before"
    init: echo "running -init"
    command: echo "running -command"

  - name: Terminal A
    init: ./mvnw install -DskipTests=false
    

  - name: Terminal B
    init: docker pull mongo
    command: docker run -p 27017:27017 -v /workspace/datadir:/data/db -d mongo

  - name: Terminal D
    init: docker run --name nsome-mysql -v /workspace/datadirmysql1:/var/lib/mysql -e MYSQL_ALLOW_EMPTY_PASSWORD=yes -d mysql


  - name: Terminal C
    init: docker run -d --hostname knk-rabbit --name my-rabbit -p 8080:15672 -p 5671:5671 -p 5672:5672 rabbitmq:3-management
   
  

  

